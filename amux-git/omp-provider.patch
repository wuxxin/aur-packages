diff --git a/amux b/amux
index e4784fd2..59e22e26 100755
--- a/amux
+++ b/amux
@@ -32,7 +32,7 @@ if [[ ! -d "$HOME/.amux" ]]; then
   done
 fi
 
-CC_HOME="${CC_HOME:-$HOME/.amux}"
+CC_HOME="${AMUX_HOME:-${CC_HOME:-$HOME/.amux}}"
 CC_SESSIONS="$CC_HOME/sessions"
 CC_DEFAULTS="$CC_HOME/defaults.env"
 
@@ -537,7 +537,7 @@ cmd_start() {
     return
   fi
 
-  # Build command. Check CC_PROVIDER to pick codex vs claude.
+  # Build command. Check CC_PROVIDER to pick omp vs codex vs claude.
   local provider="${CC_PROVIDER:-claude}"
   local cmd
   if [[ "$provider" == "codex" ]]; then
@@ -558,11 +558,13 @@ cmd_start() {
     # Ollama daemon. The model comes from CC_MODEL (default qwen3.8:27b) — CC_FLAGS
     # can add extra codex flags (approval policy, add-dir, etc.) just like codex.
     cmd="codex --oss --local-provider ollama --model ${CC_MODEL:-qwen3.8:27b}"
+  elif [[ "$provider" == "omp" || "$provider" == "oh-my-pi" ]]; then
+    cmd="omp"
   else
     cmd="${AMUX_CLAUDE_CMD:-claude}"
   fi
-  # Apply global defaults from defaults.env (skip for codex/ollama — Claude flags don't apply)
-  if [[ "$provider" != "codex" && "$provider" != "ollama" && -n "${CC_DEFAULT_FLAGS:-}" ]]; then
+  # Apply global defaults from defaults.env (skip for codex/ollama/omp — Claude flags don't apply)
+  if [[ "$provider" != "codex" && "$provider" != "ollama" && "$provider" != "omp" && "$provider" != "oh-my-pi" && -n "${CC_DEFAULT_FLAGS:-}" ]]; then
     local q
     q=$(quote_flags "$CC_DEFAULT_FLAGS") || die "failed to quote CC_DEFAULT_FLAGS"
     [[ -n "$q" ]] && cmd="$cmd $q"
@@ -584,7 +586,7 @@ cmd_start() {
     [[ -n "$q" ]] && cmd="$cmd $q"
   fi
 
-  # Codex-specific defaults (apply to ollama too — it now runs codex under the hood)
+  # Provider-specific defaults
   if [[ "$provider" == "codex" || "$provider" == "ollama" ]]; then
     # For codex (cloud): default to gpt-5.5 when no model is set.
     # For ollama: model is already set from CC_MODEL above — no default needed.
@@ -603,6 +605,10 @@ cmd_start() {
         [[ -d "$git_root/.git" ]] && cmd="$cmd --add-dir $(printf '%q' "$git_root/.git")"
       fi
     fi
+  elif [[ "$provider" == "omp" || "$provider" == "oh-my-pi" ]]; then
+    if [[ "$cmd" != *"--model"* ]]; then
+      cmd="$cmd --model ${CC_MODEL:-llama.cpp/qwen3}"
+    fi
   else
     [[ "$cmd" == *"--model"* ]] || cmd="$cmd --model sonnet"
   fi
@@ -613,7 +619,7 @@ cmd_start() {
   # to prevent auth conflict — BYO-key users don't have oauthAccount so they're
   # unaffected.
   local shell_setup=""
-  if [[ "$provider" != "codex" && "$provider" != "ollama" ]]; then
+  if [[ "$provider" != "codex" && "$provider" != "ollama" && "$provider" != "omp" && "$provider" != "oh-my-pi" ]]; then
     shell_setup="unset CLAUDECODE CLAUDE_CODE_ENTRYPOINT; "
     if [[ -f "$HOME/.claude.json" ]] && grep -q '"oauthAccount"' "$HOME/.claude.json" 2>/dev/null; then
       shell_setup="${shell_setup}unset ANTHROPIC_API_KEY; "
diff --git a/crates/amux-dashboard/static/app.js b/crates/amux-dashboard/static/app.js
index a899948b..63eca4e9 100644
--- a/crates/amux-dashboard/static/app.js
+++ b/crates/amux-dashboard/static/app.js
@@ -2705,18 +2705,20 @@ function providerLabel(provider) {
   if (provider === 'gemini') return 'Gemini';
   if (provider === 'ollama') return 'Ollama';
   if (provider === 'iterm2') return 'iTerm2';
+  if (provider === 'omp' || provider === 'oh-my-pi') return 'Oh-My-Pi';
   return 'Claude';
 }
 
 function sessionProvider(s) {
   const p = ((s && s.provider) || 'claude').toLowerCase();
-  return (p === 'codex' || p === 'gemini' || p === 'ollama' || p === 'iterm2') ? p : 'claude';
+  return (p === 'codex' || p === 'gemini' || p === 'ollama' || p === 'iterm2' || p === 'omp' || p === 'oh-my-pi') ? (p === 'oh-my-pi' ? 'omp' : p) : 'claude';
 }
 
 function providerDefaultModel(provider) {
   if (provider === 'codex') return 'gpt-5.5';
   if (provider === 'gemini') return 'auto';
   if (provider === 'ollama') return 'qwen3.8:27b';
+  if (provider === 'omp' || provider === 'oh-my-pi') return 'llama.cpp/qwen3';
   return window._AMUX_DEFAULT_MODEL || 'sonnet';
 }
 
@@ -4741,37 +4743,45 @@ function editField(session, field, current, provider) {
       {v:'gemini-2.5-flash',l:'gemini-2.5-flash'},{v:'gemini-2.5-flash-lite',l:'gemini-2.5-flash-lite'},
       {v:'gemini-3-pro-preview',l:'gemini-3-pro-preview'},{v:'gemini-3-flash-preview',l:'gemini-3-flash-preview'}
     ];
-    if (provider === 'ollama') {
-      sel.innerHTML = '<option value="">Loading local models…</option>';
-      inpWrap.style.display = 'none';
+
+    inpWrap.style.display = '';
+    sel.innerHTML = '';
+    const defOpt = document.createElement('option');
+    defOpt.value = '';
+    defOpt.textContent = '-- Preset models (or type custom above) --';
+    sel.appendChild(defOpt);
+
+    if (provider === 'omp' || provider === 'oh-my-pi') {
+      inp.value = current || 'llama.cpp/qwen3';
+      inp.placeholder = 'Custom model name (e.g. llama.cpp/qwen3)';
+      const o1 = document.createElement('option'); o1.value = 'llama.cpp/qwen3'; o1.textContent = 'llama.cpp/qwen3 (default)'; sel.appendChild(o1);
+      const o2 = document.createElement('option'); o2.value = 'auto'; o2.textContent = 'auto'; sel.appendChild(o2);
+      sel.style.display = 'block';
+      sel.value = '';
+    } else if (provider === 'ollama') {
+      inp.value = current || '';
+      inp.placeholder = 'Custom model name (e.g. qwen3.8:27b)';
       sel.style.display = 'block';
       fetch(API + '/api/ollama/models', { headers: _authHeaders() })
         .then(r => r.json())
         .then(d => {
-          sel.innerHTML = '';
           const mlist = d.models || [];
-          if (!mlist.length) {
-            const o = document.createElement('option'); o.value = ''; o.textContent = 'No local models found'; sel.appendChild(o);
-          }
           mlist.forEach(name => {
             const o = document.createElement('option'); o.value = name; o.textContent = name; sel.appendChild(o);
           });
-          sel.value = current || (mlist[0] || '');
         })
-        .catch(() => { sel.innerHTML = '<option value="">Could not reach Ollama</option>'; });
+        .catch(() => {});
     } else {
-    const models = provider === 'codex' ? codexModels : (provider === 'gemini' ? geminiModels : claudeModels);
-    sel.innerHTML = '';
-    models.forEach(m => { const o = document.createElement('option'); o.value = m.v; o.textContent = m.l; sel.appendChild(o); });
-    inpWrap.style.display = 'none';
-    sel.style.display = 'block';
-    sel.value = current || '';
-    if (current && !Array.from(sel.options).some(o => o.value === current)) {
-      const opt = document.createElement('option');
-      opt.value = current; opt.textContent = current;
-      sel.appendChild(opt);
-      sel.value = current;
-    }
+      const models = provider === 'codex' ? codexModels : (provider === 'gemini' ? geminiModels : claudeModels);
+      inp.value = current || '';
+      inp.placeholder = 'Custom model name (e.g. ' + (provider === 'codex' ? 'gpt-5.5' : (provider === 'gemini' ? 'gemini-2.5-pro' : 'sonnet')) + ')';
+      models.forEach(m => {
+        if (m.v) {
+          const o = document.createElement('option'); o.value = m.v; o.textContent = m.l; sel.appendChild(o);
+        }
+      });
+      sel.style.display = 'block';
+      sel.value = '';
     }
     // Reasoning effort — Claude only. Pre-fill from the session's current --effort flag.
     const effortWrap = document.getElementById('edit-effort-wrap');
@@ -4838,7 +4848,7 @@ function editField(session, field, current, provider) {
   }
   document.getElementById('edit-overlay').classList.add('active');
   editState = { session, field };
-  if (field !== 'model' && field !== 'provider' && field !== 'effort') setTimeout(() => { inp.focus({ preventScroll: true }); inp.select(); }, 100);
+  if (field !== 'provider' && field !== 'effort') setTimeout(() => { inp.focus({ preventScroll: true }); inp.select(); }, 100);
 }
 function closeEdit() {
   document.getElementById('edit-overlay').classList.remove('active');
@@ -4850,15 +4860,21 @@ function closeEdit() {
   tagAcItems = []; tagAcSelected = -1;
   editState = null;
 }
-// edit-select auto-submits for quick single-select fields (provider), but the
-// model field pairs with the reasoning-effort dial, so it waits for Save.
+// edit-select auto-submits for quick single-select fields (provider, effort),
+// but for the model field it populates the custom input box so the user can adjust.
 function _editSelectChanged() {
-  if (editState && editState.field === 'model') return;
+  if (editState && editState.field === 'model') {
+    const selVal = document.getElementById('edit-select').value;
+    if (selVal !== undefined && selVal !== '') {
+      document.getElementById('edit-input').value = selVal;
+    }
+    return;
+  }
   submitEdit();
 }
 async function submitEdit() {
   if (!editState) return;
-  const val = (editState.field === 'model' || editState.field === 'provider' || editState.field === 'effort')
+  const val = (editState.field === 'provider' || editState.field === 'effort')
     ? document.getElementById('edit-select').value.trim()
     : document.getElementById('edit-input').value.trim();
   if (!val && editState.field !== 'desc' && editState.field !== 'tags' && editState.field !== 'model' && editState.field !== 'task' && editState.field !== 'effort') return;
diff --git a/crates/amux-server/src/api/session_verbs.rs b/crates/amux-server/src/api/session_verbs.rs
index f7ba52a7..81565c5f 100644
--- a/crates/amux-server/src/api/session_verbs.rs
+++ b/crates/amux-server/src/api/session_verbs.rs
@@ -2206,11 +2206,12 @@ fn render_session_transcript(name: &str, max_chars: usize) -> String {
 // contract).
 // ---------------------------------------------------------------------------
 
-pub const SESSION_PROVIDERS: [&str; 5] = ["claude", "codex", "gemini", "iterm2", "ollama"];
-const PROVIDER_YOLO_FLAGS: [&str; 3] = [
+pub const SESSION_PROVIDERS: [&str; 6] = ["claude", "codex", "gemini", "iterm2", "ollama", "omp"];
+const PROVIDER_YOLO_FLAGS: [&str; 4] = [
     "--dangerously-skip-permissions",
     "--dangerously-bypass-approvals-and-sandbox",
     "--yolo",
+    "--approval-mode=yolo",
 ];
 const VALID_EFFORTS: [&str; 5] = ["low", "medium", "high", "xhigh", "max"];
 
@@ -2554,6 +2555,7 @@ fn default_model_for_provider(provider: &str) -> String {
         // Ollama runs via `codex --oss --local-provider ollama --model <model>`.
         // A launchable default is required (this box has qwen3.8:27b pulled).
         "ollama" => "qwen3.8:27b".into(),
+        "omp" | "oh-my-pi" => "llama.cpp/qwen3".into(),
         _ => get_default_model(),
     }
 }
@@ -2576,6 +2578,7 @@ pub fn launch_base_binary(provider: &str) -> &'static str {
         // ollama runs codex under the hood (`--oss --local-provider ollama`).
         "codex" | "ollama" => "codex",
         "gemini" => "gemini",
+        "omp" | "oh-my-pi" => "omp",
         // claude, iterm2, and anything unknown launch via build_claude_cmd,
         // whose default binary is `claude` (overridable by AMUX_CLAUDE_CMD).
         _ => "claude",
@@ -5604,6 +5607,25 @@ async fn start_session(state: &AppState, name: &str, extra_flags: &str, skip_con
             }
             format!("{base_bin}{opts}")
         }
+        "omp" | "oh-my-pi" => {
+            let mut opts = String::new();
+            if !flags.is_empty() {
+                opts += &format!(" {}", shell_quote_flags(&flags));
+            }
+            if !extra_flags.is_empty() {
+                opts += &format!(" {}", shell_quote_flags(extra_flags));
+            }
+            let cc_model = cfg.get_or("CC_MODEL", "").trim().to_string();
+            if !opts.contains("--model") {
+                let model_to_use = if !cc_model.is_empty() {
+                    cc_model
+                } else {
+                    default_model_for_provider("omp")
+                };
+                opts += &format!(" --model {}", sh_quote(&model_to_use));
+            }
+            format!("{base_bin}{opts}")
+        }
         _ => build_claude_cmd(&cfg, &flags, &default_flags, &session_flag, extra_flags),
     };
 
@@ -5611,7 +5633,7 @@ async fn start_session(state: &AppState, name: &str, extra_flags: &str, skip_con
     // cd, source the global agent credentials.
     let mut has_oauth = false;
     let mut shell_rc = String::new();
-    if provider != "codex" && provider != "gemini" && provider != "ollama" {
+    if provider != "codex" && provider != "gemini" && provider != "ollama" && provider != "omp" && provider != "oh-my-pi" {
         shell_rc.push_str("unset CLAUDECODE CLAUDE_CODE_ENTRYPOINT; ");
         if let Ok(t) = std::fs::read_to_string(PathBuf::from(std::env::var("HOME").unwrap_or_default()).join(".claude.json")) {
             if let Ok(v) = serde_json::from_str::<Value>(&t) {
diff --git a/crates/amux-server/src/api/sessions_legacy.rs b/crates/amux-server/src/api/sessions_legacy.rs
index 0fb4a6a6..3b771be5 100644
--- a/crates/amux-server/src/api/sessions_legacy.rs
+++ b/crates/amux-server/src/api/sessions_legacy.rs
@@ -473,11 +473,20 @@ impl FleetSignals {
             ])
             .output();
         match &tmux_out {
-            Ok(o) if !o.status.success() => tracing::warn!(
-                status = %o.status,
-                stderr = %String::from_utf8_lossy(&o.stderr).trim(),
-                "tmux list-sessions failed — fleet will read as not-running"
-            ),
+            Ok(o) if !o.status.success() => {
+                let stderr = String::from_utf8_lossy(&o.stderr);
+                if !stderr.contains("no server")
+                    && !stderr.contains("No such file")
+                    && !stderr.contains("error connecting to")
+                    && !stderr.contains("no sessions")
+                {
+                    tracing::warn!(
+                        status = %o.status,
+                        stderr = %stderr.trim(),
+                        "tmux list-sessions failed — fleet will read as not-running"
+                    );
+                }
+            }
             Err(e) => tracing::warn!(
                 error = %e,
                 "tmux spawn failed — fleet will read as not-running"
@@ -1654,8 +1663,15 @@ pub(crate) fn worker_model_env(
     default_model: &str,
 ) -> (String, String, String) {
     let is_ollama = provider == "ollama";
+    let is_omp = provider == "omp" || provider == "oh-my-pi";
     let model = if is_ollama {
         raw_model.to_string()
+    } else if is_omp {
+        if raw_model.is_empty() {
+            "llama.cpp/qwen3".to_string()
+        } else {
+            raw_model.to_string()
+        }
     } else if raw_model.is_empty() {
         default_model.to_string()
     } else {
@@ -1668,7 +1684,7 @@ pub(crate) fn worker_model_env(
     } else {
         String::new()
     };
-    let cc_model = if is_ollama { model.clone() } else { String::new() };
+    let cc_model = if is_ollama || is_omp { model.clone() } else { String::new() };
     let resolved_model = if is_ollama {
         model
     } else {
